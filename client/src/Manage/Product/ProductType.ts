export type Product = {
    createdAt: number,
    description: string,
    featured: boolean,
    id: number,
    images: string[],
    name: string,
    reviews: number[],
    stock: number,
    updatedAt: number
}